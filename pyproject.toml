[project]
name = "labs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
    "typer>=0.27.2",
]
[tool.mypy]
mypy_path = "src"
explicit_package_bases = true
